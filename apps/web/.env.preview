# 只读预览配置
#
# `npm run dev:preview` / `npm run build:preview` 会以 --mode preview 启动，
# 从而加载本文件。
#
# 效果：不起后端、无需登录，可以浏览每一个页面，但所有写操作被禁用
# （标记进度、提交自测、注册）。用于把界面展示给别人看，而不是给别人用。
VITE_AIFS_DEMO=1
VITE_DEMO_READONLY=1
